import 'package:flutter/material.dart';

class SynopsisScreen extends StatelessWidget {
  const SynopsisScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Synopsis Screen'),
    );
  }
}
